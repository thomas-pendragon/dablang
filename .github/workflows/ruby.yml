name: CI

on:
  pull_request:
    branches: [master]

concurrency:
  group: ci-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  public-site:
    runs-on: ubuntu-latest
    env:
      BUNDLE_GEMFILE: docs/Gemfile

    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.12'
          bundler-cache: true
      - name: Validate and build public site
        run: bundle exec ruby script/public_site.rb

  test:
    runs-on: ubuntu-latest
    env:
      PREMAKE: ./premake5
    strategy:
      fail-fast: false
      matrix:
        ruby-version: ['3.3.12', '3.4.9', '4.0.5']

    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true
      - name: Download Premake
        run: wget https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz
      - name: Install Premake
        run: |
          tar xf premake-5.0.0-beta8-linux.tar.gz
          chmod +x premake5
      - name: Run complete validation gate
        run: ruby script/complete_gate.rb

  test-macos:
    runs-on: macos-15-intel
    env:
      PREMAKE: ./premake5

    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.12'
          bundler-cache: true
      - name: Download Premake
        run: curl --fail --location --retry 3 --output premake-5.0.0-beta8-macosx-x64.tar.gz https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-macosx-x64.tar.gz
      - name: Install Premake
        run: |
          tar xf premake-5.0.0-beta8-macosx-x64.tar.gz
          chmod +x premake5
          echo "CLANG_FORMAT=$(brew --prefix llvm@18)/bin/clang-format" >> "$GITHUB_ENV"
      - name: Run complete validation gate
        run: ruby script/complete_gate.rb

  test-windows:
    runs-on: windows-latest
    env:
      PREMAKE: .\premake5.exe

    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.12'
          bundler-cache: true
      - name: Download Premake
        shell: pwsh
        run: curl.exe --fail --location --retry 3 --output premake-5.0.0-beta8-windows.zip https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-windows.zip
      - name: Install Premake
        shell: pwsh
        run: Expand-Archive -LiteralPath premake-5.0.0-beta8-windows.zip -DestinationPath .
      - name: Run complete validation gate
        shell: pwsh
        run: ruby script/complete_gate.rb

  address-sanitizer:
    runs-on: ubuntu-24.04
    env:
      PREMAKE: ./premake5
      CC: clang-18
      CXX: clang++-18

    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.12'
          bundler-cache: true
      - name: Install AddressSanitizer compiler
        run: |
          sudo apt-get update
          sudo apt-get install --yes clang-18
      - name: Download Premake
        run: wget https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz
      - name: Install Premake
        run: |
          tar xf premake-5.0.0-beta8-linux.tar.gz
          chmod +x premake5
      - name: Run AddressSanitizer validation gate
        run: bundle exec rake address_sanitizer_spec

  undefined-behavior-sanitizer:
    runs-on: ubuntu-24.04
    env:
      PREMAKE: ./premake5
      CC: clang-18
      CXX: clang++-18

    steps:
      - uses: actions/checkout@v7
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.3.12'
          bundler-cache: true
      - name: Install UndefinedBehaviorSanitizer compiler
        run: |
          sudo apt-get update
          sudo apt-get install --yes clang-18
      - name: Download Premake
        run: wget https://github.com/premake/premake-core/releases/download/v5.0.0-beta8/premake-5.0.0-beta8-linux.tar.gz
      - name: Install Premake
        run: |
          tar xf premake-5.0.0-beta8-linux.tar.gz
          chmod +x premake5
      - name: Run UndefinedBehaviorSanitizer validation gate
        run: bundle exec rake undefined_behavior_sanitizer_spec
