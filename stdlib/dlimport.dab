func import_libc(method, name = nil) {
  #puts("Import libc method '" + method.to_s + "' (" + name.to_s + ")");
  if (name == nil) {
    name = method.name;
  }
  __dlimport("$LIBC", method, name);
}
